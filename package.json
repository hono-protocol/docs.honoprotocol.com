{
  "name": "hono-protocol",
  "version": "1.0.0",
  "description": "Documentation",
  "main": "index.js",
  "scripts": {
    "build": "gitbook build && echo 'docs.honoprotocol.com' > _book/CNAME && node cleanup-paths.js && node insert-gtag.js ./_book && node prepend-title.js ./_book && node update-site.js && node replace-gitbook-links.js",
    "deploy": "gh-pages -d _book"
  },
  "author": "Developer",
  "license": "MIT",
  "dependencies": {
    "graceful-fs": "^4.2.11"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
